/* Use a notação de colchetes para encontrar o primeiro caractere em uma string

A notação de colchetes é uma forma de obter um caractere em um índice específico dentro de uma string.
A maioria das linguagens de programação modernas, como JavaScript, não começa a contar em 1 como os humanos fazem.
Eles começam em 0. Isso é conhecido como indexação baseada em zero.

Por exemplo, o caractere no índice 0 na palavra "Charles" é "C". Portanto, se var firstName = "Charles",
você pode obter o valor da primeira letra da string usando firstName [0].

Exemplo: */

var firstName = "Charles";
var firstLetter = firstName[0]; // firstLetter é "C"

/*Use a notação de colchetes para localizar o primeiro caractere na variável lastName e atribua-o a firstLetterOfLastName.

EXERCÍCIO RESOLVIDO ABAIXO: */ 

// Setup
var firstLetterOfLastName = "";
var lastName = "Lovelace";

// Only change code below this line
firstLetterOfLastName = lastName[0]; // Change this line

// 

var nome = "DIEGO Rodrigues"
console.log(nome[0])
console.log(nome[1])
console.log(nome[2])
console.log(nome[3])
console.log(nome[4])