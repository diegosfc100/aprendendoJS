// OPERADORES E COMPARADORES:

    // var++  incremento, soma + 1
    // var--  decremento, diminui -1

    var a = 0;
    var b = 0;
    console.log(++a); // pré incremento(imprime o 'a' já incrementado) -> 1
    console.log(b++); // pós incremento(imprime o 'b' e depois incrementa) -> 0
    console.log(b); // 'b' com incremento -> 1

    
    //OPERADORES ARITMÉTICOS ABREVIADOS
        // +=, -=, *=, /=
        //(var = var + 20) === (var += 20)
        //(var = var * 20) === (var *= 20)

        // =  atribuição
        // ==  igual
        // ===  igual e do mesmo tipo
        // !=  diferente
        // !==  diferente, mas do mesmo tipo